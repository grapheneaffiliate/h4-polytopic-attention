# SB26
**Status:** 0/8 unsolved | **Tags:** keyboard_click | **Baselines:** 18, 16, 15, 15, 31, 24, 17, 17

## Mechanic
Tile matching puzzle with energy constraint. ACTION5=confirm (costs 1 energy), ACTION6=click, ACTION7=undo. 64 energy pool. Must match all tiles to targets before energy runs out.

## Win Condition
`lmvwmlqtw > 15` after all targets filled. Energy `sjcuorclg > 0`.

## Why Unsolved
- 10 effective actions (1 confirm + 8 clicks + 1 undo)
- 2017 unique states explored in 300s but baseline 18 not reached
- BFS rate: 34 states/s (deepcopy bottleneck)
- Clicks at y=57 change frame, clicks at y=29 don't

## Key Variables
- `sjcuorclg`: energy (starts at 64, -1 per ACTION5)
- `buvfjfmpp`: puzzle sequence state
- `pmygakdvy`: target box index
- `uvawsoycr`: undo history stack

## What's Needed
CSP/ILP solver: model the tile matching as a constraint satisfaction problem. The puzzle has a tree structure (buvfjfmpp tracks depth). Understanding the exact matching rules from source would enable a direct solver.

## Source
`environment_files/sb26/<hash>/sb26.py`
