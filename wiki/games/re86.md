# RE86
**Status:** 0/8 unsolved | **Tags:** keyboard_click | **Baselines:** 28, 38, 198, 57, 84, 117, 328, 221

## Mechanic
Fill/clone puzzle. Manipulate sprites to fill a canvas. Win when sprite pixels match canvas reference.

## Win Condition
`cdjxpfqest()` validates sprite pixels match canvas reference (pixel-level comparison).

## Why Unsolved
- 5 keyboard actions, no dynamic clicks
- 41K states explored at depth 8-9, baseline 28 — not deep enough
- IDDFS reached depth 8 in 300s at 425 states/s — baseline 28 needs 5^28 states
- Explorer v6 gets 0-1/8 with randomization variance

## What's Needed
The game requires understanding the canvas-filling mechanic at the pixel level. The `cdjxpfqest()` function compares 2D pixel arrays — need to reverse-engineer what the target canvas looks like and compute the action sequence to produce it.

## Source
`environment_files/re86/<hash>/re86.py`
